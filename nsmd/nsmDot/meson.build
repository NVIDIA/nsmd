# nsmDot sources are added in the parent meson.build file
# This subdirectory meson.build is kept for consistency with other subdirectories

nsmd_headers += ['.', '..']

if get_option('tests').enabled()
    subdir('test')
endif


