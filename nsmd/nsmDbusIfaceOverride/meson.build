sources += [
    'nsmDbusIfaceOverride/nsmWriteProtectedIntf.cpp',
    'nsmDbusIfaceOverride/nsmModeIntf.cpp',
]

nsmd_headers += ['.', '..', 'nsmDbusIfaceOverride']

if get_option('tests').enabled()
    subdir('test')
endif